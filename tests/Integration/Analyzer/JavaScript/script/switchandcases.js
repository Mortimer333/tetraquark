switch (expression) {
    case expression:

        break;
    default:

}

switch(){}
switch (case2) {
    case 1:
        'case'
    case 2:
        'case 2'
        break;
    default:
        'default'
}

switch (expression) {
    case expression:
        let a = function () {
            switch (expression2) {
                case expression2:

                    break;
                default:

            }
        }
        break;
    default:

}
