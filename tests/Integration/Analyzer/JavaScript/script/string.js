'single apo'
"double apo"
`template`

'single "with" double'
'single `with` template'
"double 'with' single"
"double `with` template"
`template "with" double`
`template 'with' single`

'single \'escape'
"double \"escpae"
`template \`escape`

`${var2}str${variable}str`
`${(function() {return `string in function in string`})()}str${variable}str`
