vars.lan.bacn['asd' + asd['add']] = atr.asd('asd')(2,56).as
let attrs = letterSet.attrs ?? { style : 'color:#FFF;' };
let middleLines = this.get.clone(clipboard.slice( 1, clipboard.length - 1 ));
let lastLetter, lastChildIndex;
